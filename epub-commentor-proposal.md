# AI 智能评注 EPUB 电子书制作工具项目技术提案

> 本项目暂处于意向阶段，对实际实现方式的可行性调查暂未执行。后续应当根据实际需求和可行性进行修改以适应现实情况。

## 一、 项目背景与核心痛点

在阅读外文或学术类电子书，抑或是严肃文学等时，读者常面临理解成本高、上下文关联复杂的挑战，同时阅读过程枯燥、无聊，注意力容易分散。现有的 AI 翻译或阅读工具多采用逐句逐段机械翻译的模式，不仅破坏了原书的阅读节奏，也无法提供类似传统书籍中“夹注”或“总评”的深度导读体验。

本项目旨在开发一款基于现有的开源 EPUB 翻译器（epub-translator）框架进行二次开发的轻量级工具。通过调整 AI 交互策略与 HTML 注入逻辑，将全章感知、按话题块智能评注以及字词级微观夹注的能力赋予电子书，最终生成可直接导入 Kindle 等墨水屏设备阅读的高质量评注版 EPUB 文件。

## 二、 核心功能设计

### 1. 全局与全章内容感知

为避免 AI 出现断章取义或前后术语不一致的现象，采用两阶段处理机制：

- 阶段一（全章扫读）：在处理具体段落前，先将全章文本（或目录、前言）输入大模型，生成该章节的核心线索、逻辑大纲与高频术语备忘录。
- 阶段二（局部批注）：将该备忘录作为系统提示词（System Prompt）的一部分，约束并引导后续的段落级批注，使其具备全局视野。

此外，还将在系统提示词的设计中预留一个可选的部分，运行用户自定义输入该书的简介，以加强 AI 理解。

### 2. 连续段落的话题块切分与批注

打破逐段批注的机械限制，采用滑动窗口方式处理文本：

每次向 AI 输送包含多个（如 5 到 8 个，可配置）段落的 HTML 文本块，并为每个标签赋予临时的唯一标识（如 p_id）。

允许 AI 自主判断内容关联度。若连续数段讨论同一主题，AI 应当返回合并后的单一批注，并明确指定该批注的挂载目标及覆盖的段落范围。

允许 AI 对平铺直叙、无需解释的段落直接忽略，以保持阅读的流畅性。

### 3. 灵活的批注位置控制

根据用户的阅读习惯，系统支持配置两种批注挂载模式：

- 前置导读模式：将 AI 生成的话题块总结插入到该话题第一段的前方，帮助读者预知背景。
- 后置总结模式：将批注插入到该话题最后一段的后方，用于提炼核心观点与微言大义。

## 三、 技术实现路径与改造策略

### 1. 二次开发选型

项目将基于现有的开源 EPUB 翻译器项目（如 oomol-lab/epub-translator）进行外科手术式改造，放弃重写底层 ZIP 解压、XML 目录解析、HTML 标签清洗以及标准 EPUB 重新打包等通用模块，从而将研发精力集中于核心的 AI 交互与节点注入逻辑。

### 2. 管道模式架构

为防止新逻辑与原项目的底层遍历算法高度耦合，改造采用解耦的管道模式：

- 提取层：利用原项目的解析功能，将 EPUB 内的 HTML 结构干净地提取为文本对象列表。
- 处理层（核心编写部分）：编写独立的 Python 脚本，负责段落打标、上下文打包、调用大模型 API 并解析返回的标准 JSON 数据。
- 注入层：使用 BeautifulSoup 库，根据 JSON 中的位置指示，动态创建包含特定类名（CSS Class）的新节点并插入 HTML 树。

### 3. 样式表适配

在 EPUB 的样式表（CSS）中注入专为电子墨水屏优化的排版样式。通过控制字体、字号与缩进，或增加背景等，使批注在视觉上与正文形成清晰的区分度，确保导入移动终端或 Kindle 后具备良好的阅读体验。

## 四、 项目里程碑与工作量预估

- 原型调研与 API 联调。熟悉选定开源项目的核心源码，跑通带有上下文感知的 AI 评注 Prompt，验证大模型返回 JSON 格式的稳定性。
- 核心业务逻辑编写。实现 HTML 文本的块切分算法、两阶段全局感知机制以及基于 BeautifulSoup 的节点注入函数。
- 样式调优与终端适配。设计并注入夹注和块注的 CSS 样式，在 Kindle 或平板设备上进行多类型电子书的排版测试。
- 边界情况处理与代码清洗。修复可能存在的标签破损、内存占用过高或特殊排版错位等隐形 Bug，完成项目交付。
